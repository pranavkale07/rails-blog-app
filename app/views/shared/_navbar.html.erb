<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand fw-bold" href="<%= root_path %>">Blogify</a>

        <!-- Toggle button for mobile view -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
                data-bs-target="#navbarNav" aria-controls="navbarNav" 
                aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Links -->
        <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
            <li class="nav-item">
            <%= link_to "Blogs", blogs_path, class: "nav-link" %>
            </li>
            <% if logged_in? %>
            <li class="nav-item">
                <%= link_to "My Blogs", my_blogs_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to "Profile", current_user, class: "nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to "Logout", logout_path, method: :delete, 
                    data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to log out?" }, 
                    class: "nav-link text-danger" %>
            </li>
            <% else %>
            <li class="nav-item">
                <%= link_to "Login", login_path, class: "nav-link btn btn-primary px-3 mx-2 text-white" %>
            </li>
            <% end %>
        </ul>
        </div>
    </div>
</nav>